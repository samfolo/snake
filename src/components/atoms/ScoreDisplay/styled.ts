import styled from 'styled-components';

export const ScoreDisplayContainer = styled.div`
  color: ${(p) => p.theme.palette.paletteSecondary};
  font-size: ${(p) => p.theme.fundamentals.fontSizeJumbo};
`;
